package ai.bitlabs.sdk.util

import androidx.core.content.FileProvider

class CustomFileProvider : FileProvider() {
    // This class is intentionally left empty
    // Use to avoid Provider Conflicts
}
